'use client';
import { useData, Loading, Failure, PageHead, num, kinds, date, Empty } from './ui';
import { Progress } from '@/components/ui/progress';
import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from '@/components/ui/table';
export default function Stats() { const { data: d, loading, error, reload } = useData('stats'); if (loading)
    return <Loading />; if (error)
    return <Failure error={error} retry={reload}/>; return <><PageHead eyebrow="DATOS DE TU ESPACIO" title="Estadísticas" description="Consulta el avance y la actividad de tus proyectos guardados."/><div className="stat-grid">{[[d.words, 'Palabras escritas'], [d.chapters, 'Capítulos'], [d.sources, 'Fuentes'], [d.memories, 'Entradas de memoria']].map(([n, label]) => <article key={label}><span>{label}</span><strong>{num(n)}</strong></article>)}</div><section className="surface"><div className="section-top"><div><h2>Avance por proyecto</h2><p>Palabras actuales frente a la meta que elegiste. Incluye proyectos archivados.</p></div></div>{d.projects.length ? <Table><TableHeader><TableRow><TableHead>Proyecto</TableHead><TableHead>Palabras</TableHead><TableHead>Meta</TableHead><TableHead>Avance</TableHead></TableRow></TableHeader><TableBody>{d.projects.map((p: any) => <TableRow key={p.id}><TableCell><a className="table-title" href={'/proyectos/' + p.id}>{p.title}</a><small>{kinds[p.kind]}{p.archived ? ' · Archivado' : ''}</small></TableCell><TableCell>{num(p.words)}</TableCell><TableCell>{num(p.goal)}</TableCell><TableCell><div className="progress-cell"><Progress value={Math.min(100, p.words / p.goal * 100)} aria-label={'Avance de ' + p.title}/><span>{Math.round(p.words / p.goal * 100)}%</span></div></TableCell></TableRow>)}</TableBody></Table> : <Empty title="El progreso comienza con tu primer proyecto."><a className="btn" href="/proyectos">Crear un proyecto</a></Empty>}</section><div className="stats-bottom"><section className="surface"><h2>Actividad de escritura</h2><p>Versiones guardadas por día durante los últimos 30 días (UTC). No equivale a palabras nuevas. Se excluyen capítulos eliminados.</p>{d.activity.length ? <Table><TableHeader><TableRow><TableHead>Fecha</TableHead><TableHead>Guardados</TableHead></TableRow></TableHeader><TableBody>{[...d.activity].reverse().map((a: any) => <TableRow key={a.day}><TableCell>{date(a.day + 'T12:00:00Z')}</TableCell><TableCell>{num(a.saves)}</TableCell></TableRow>)}</TableBody></Table> : <p className="quiet-note">Aún no hay versiones guardadas.</p>}</section><section className="surface"><h2>Uso de recursos</h2><dl className="resource-stats"><div><dt>Texto de fuentes almacenado</dt><dd>{num(Math.ceil(d.bytes / 1024))} KB</dd></div><div><dt>Solicitudes a IA</dt><dd>0</dd></div><div><dt>Costo de IA</dt><dd>Sin consumo</dd></div></dl><p>La IA no está conectada. Los archivos y metadatos de tu proyecto se guardan en el servidor.</p><a className="link-button" href="/ajustes">Ver configuración</a></section></div></>; }
