'use client';
import { useState } from 'react';
import { ArrowRight, CircleHelp, Feather, FileText, Save } from 'lucide-react';
import { Modal } from './ui';
const steps = [{ title: 'Empieza por tu voz', copy: 'Añade muestras de escritura y elige tus preferencias. Revisa el perfil antes de aprobarlo: tú decides qué representa tu estilo.', Icon: Feather, href: '/mi-voz', action: 'Configurar mi voz' }, { title: 'Dale un lugar a tu idea', copy: 'Crea un proyecto, añade capítulos y reúne tus fuentes. Cada obra tiene sus propias notas y reglas.', Icon: FileText, href: '/proyectos?nuevo=1', action: 'Crear un proyecto' }, { title: 'Conserva cada avance', copy: 'Pulsa Guardar para crear una versión en tu cuenta. Si cierras la página por accidente, NOVA puede ofrecerte la copia de recuperación del navegador.', Icon: Save, href: '/proyectos', action: 'Ir a mis proyectos' }];
export default function QuickGuide() { const [open, setOpen] = useState(false), [step, setStep] = useState(0); const current = steps[step]; return <><button className="guide-trigger" onClick={() => { setStep(0); setOpen(true); }}><CircleHelp size={16}/>Guía rápida</button><Modal open={open} onOpenChange={setOpen} title="Tu primer recorrido" description="Tres pasos para empezar a escribir en NOVA."><div className="guide-step" key={step}><current.Icon size={30} color="#6257d8"/><h3>{current.title}</h3><p>{current.copy}</p><a className="help-link" href={current.href}>{current.action}<ArrowRight size={16}/></a></div><div className="guide-controls"><span>{step + 1} de {steps.length}</span><div className="pilot-actions">{step > 0 && <button className="btn secondary" onClick={() => setStep(step - 1)}>Atrás</button>}<button className="btn" onClick={() => step === steps.length - 1 ? setOpen(false) : setStep(step + 1)}>{step === steps.length - 1 ? 'Listo' : 'Siguiente'}<ArrowRight size={16}/></button></div></div></Modal></>; }
